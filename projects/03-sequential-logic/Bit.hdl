CHIP Bit {
    IN in, load;
    OUT out;
    PARTS:
    DFF(in=muxOut, out=dffOut, out=out);
    Mux(a=dffOut, b=in, sel=load, out=muxOut);
}

/*
DFF => Data Flop-Flips Concept  + Clock 
if load(t)  then out(n+1) = in(n)
else out(n+1)= out(n)
*/